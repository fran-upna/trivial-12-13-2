// Prototipos de las funciones de respuestas.c

// Comprobamos si la respuesta enviada es correcta o no
void comprobar_respuesta(partidas p, int correcta);

// Enviamos al servidor la respuesta del cliente a una pregunta
void responder(int socket);
